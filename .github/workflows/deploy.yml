name: Deploy MyGreekTax Brain to AWS Lambda

on:
  push:
    branches:
      - main

# The workflow only reads the repo and calls AWS. Without this block the
# default GITHUB_TOKEN carries write scope on contents, packages, issues and
# more for every run, which this deploy never needs.
permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'

    - name: Install production dependencies
      working-directory: src
      run: npm install --omit=dev

    - name: Package deployment archive
      working-directory: src
      run: zip -r ../deployment.zip index.js package.json node_modules

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: eu-north-1

    - name: Push Zip to AWS Lambda
      run: |
        aws lambda update-function-code \
          --function-name mygreektax-brain \
          --zip-file fileb://deployment.zip
