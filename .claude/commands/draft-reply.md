---
description: Draft a client or partner email under MyGreekTax rules
---

Draft an email. Input: $ARGUMENTS (who it is for, the thread or context, and the goal). If context is thin, ask for the thread before drafting.

Follow CLAUDE.md rules strictly. Sequence:

1. Classify the recipient: CLIENT (expat, English) or PARTNER/CANDIDATE (Greek accountant, Greek).
2. Pull what is relevant: matching case patterns from `cases/`, rules and SOPs from `wiki/`, service definitions from `wiki/services/`. For client quotes, retail prices from `pricing/price-table.md`. For partner drafts, NEVER open retail figures into the draft (rule R2).
3. Check the complexity triggers (R3). If any apply, the client wording stays provisional and the internal notes list exactly what the partner must confirm.
4. Draft in the correct language and register:
   - Client: plain warm English, Greek terms explained on first use, no jargon walls.
   - Partner or candidate: Greek, formal plural if the contact is new or not confirmed as established, short, one clear ask, no comparisons to other candidates.
5. If the draft is partner-facing, run the R2 scan: list every currency figure in the draft and verify each is wholesale or partner-proposed. Remove or replace anything retail.
6. Close with the exact signature from R5. No em dashes, no en dashes, any language.

Output format:

[READY TO SEND]
(subject line, then the email body)

[INTERNAL NOTES]
(assumptions made, what needs partner verification, suggested next step, R2 scan result if partner-facing)
